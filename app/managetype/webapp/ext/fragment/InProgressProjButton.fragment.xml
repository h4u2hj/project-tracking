<core:FragmentDefinition
    xmlns:core="sap.ui.core"
    xmlns="sap.m"
>
    <Button
        core:require="{ handler: 'managetype/ext/fragment/InProgressProjButton'}"
        text="{= (Number(${totalInProgressProjects}) === 0) ? 'No in-progress projects' : (Number(${totalInProgressProjects}) === 1 ? '1 in-progress project' : Number(${totalInProgressProjects}) + ' in-progress projects')}"
        press="handler.onPress"
        icon="sap-icon://in-progress"
        id="idInProgressProjButton"
    />
</core:FragmentDefinition>
