<core:FragmentDefinition
    xmlns:core="sap.ui.core"
    xmlns="sap.m"
>
    <Button
        core:require="{ handler: 'managetype/ext/fragment/FinishedProjButton'}"
        text="{= (Number(${totalFinishedProjects}) === 0) ? 'No finished projects' : (Number(${totalFinishedProjects}) === 1 ? '1 finished project' : Number(${totalFinishedProjects}) + ' finished projects')}"
        press="handler.onPress"
        icon="sap-icon://sys-enter"
        id="idFinishedProjButton"
    />
</core:FragmentDefinition>
